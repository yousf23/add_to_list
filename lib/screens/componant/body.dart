import 'package:add_to_list/screens/componant/welcome.dart';
import 'package:flutter/material.dart';

class Body extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        Welcome(name:'youssef', avata:),
      ],
    );
  }
}
